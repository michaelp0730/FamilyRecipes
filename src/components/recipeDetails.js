import React from 'react'
import FooterMain from './footerMain';

const RecipeDetails = (props) => {
    console.log('RecipeDetails props: ', props);

    return (
        <div>
            <p>Recipe details to come</p>
            <FooterMain />
        </div>
    );
};

export default RecipeDetails;
